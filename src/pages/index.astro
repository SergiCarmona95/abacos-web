const lang = Astro.request.headers.get('accept-language') || ''
const prefersCatala = lang.startsWith('ca') || lang.includes('ca-ES')
const redirect = prefersCatala ? '/ca/' : '/es/'
return Astro.redirect(redirect)